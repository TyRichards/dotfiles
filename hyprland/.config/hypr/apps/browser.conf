# Fix Brave Browser "Restore Tabs" Modal Constraint Issue
# windowrulev2 = float, class:^(brave-browser)$, title:^(Restore pages?)$
# windowrulev2 = centerwindow, class:^(brave-browser)$, title:^(Restore pages?)$

# Float and center all Brave pop-up/extension windows
# windowrulev2 = float, class:^(brave-browser)$, windowtype:^(pop-up|dialog)$
# windowrulev2 = centerwindow, class:^(brave-browser)$, windowtype:^(pop-up|dialog)$
# windowrulev2 = size 800 600, class:^(brave-browser)$, windowtype:^(pop-up|dialog)$  # Optional: Set a default size# Only a subtle opacity change, but not for video sites

# Only a subtle opacity change, but not for video sites
# windowrule = opacity 1 0.97, tag:chromium-based-browser
# windowrule = opacity 1 0.97, tag:firefox-based-browser

# Some video sites should never have opacity applied to them
# windowrule = opacity 1.0 1.0, initialTitle:((?i)(?:[a-z0-9-]+\.)*youtube\.com_/|app\.zoom\.us_/wc/home)
