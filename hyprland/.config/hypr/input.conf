# Control your input devices
# See https://wiki.hypr.land/Configuring/Variables/#input
input {
  # Use multiple keyboard layouts and switch between them with Alt + Space
  # kb_layout = us,dk
  kb_options = compose:ralt # ,grp:alt_space_toggle

  # Change speed of keyboard repeat
  repeat_rate = 40
  repeat_delay = 600

  # Increase sensitity for mouse/trackpack (default: 0)
  sensitivity = 0.25

  # Force No Accelleration
  # force_no_accel = 0

  touchpad {
    # Use natural (inverse) scrolling
    natural_scroll = true

    # Use two-finger clicks for right-click instead of lower-right corner
    clickfinger_behavior = true

    # Control the speed of your scrolling
    scroll_factor = 0.25
  }
}

# Scroll faster in the terminal
windowrule = scrolltouchpad 1.5, class:Alacritty


# Custom Settings

# device {
#     name = synps/2-synaptics-touchpadjournalctl -xe | grep hyprland
#     sensitivity = 0.5
#     accel_profile = flat
# }
#
device {
    name = ty_magic_mouse
    sensitivity = -0.75
    # accel_profile = flat #disables acceleration
    # accel_profile = flatty’s-magic-mouse
}

# device:ty_magic_mouse {
#   sensitivity = -0.5
#   # accel_profile = flat  # Disables acceleration
#   # scroll_speed = 55  # Adjust scroll speed (higher is faster)
# }
