# TODO

- [ ] Open the NuPhy firmware/VIA app and manually map the Air75 F4 Search and Print Screen keys (plus any other special keys) to unique intended keycodes, then comment out the temporary Hyprland `code:65` / `code:133` bindings in `bindings.conf`.
